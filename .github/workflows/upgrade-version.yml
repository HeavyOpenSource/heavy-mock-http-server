name: Upgrade Version and Deploy

on:
  pull_request:
    types: [ closed ]
    branches:
      - 'main'

jobs:
  upgrade-version:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Check out the repo and login with bot account
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.BOT_GITHUB_TOKEN }}

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'oracle'
          java-version: '21.0.3'
          cache: 'maven'
          cache-dependency-path: 'server/pom.xml'

      - name: Retrieve next version
        id: next-version
        shell: bash
        run: |
          VALUE=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          VERSION=$(echo $VALUE | cut -d'-' -f1)
          MAJOR=$(echo $VERSION | cut -d'.' -f1)
          MINOR=$(echo $VERSION | cut -d'.' -f2)
          PATCH=$(echo $VERSION | cut -d'.' -f3)
          
          echo "value=$VERSION" >> $GITHUB_OUTPUT
          echo "major=$MAJOR" >> $GITHUB_OUTPUT
          echo "minor=$MINOR" >> $GITHUB_OUTPUT
          echo "patch=$PATCH" >> $GITHUB_OUTPUT

      - name: Compute snapshot version
        id: snapshot-version
        shell: bash
        run: |
          MAJOR=${{ steps.next-version.outputs.value }}
          MINOR=${{ steps.next-version.outputs.minor }}
          PATCH=$((${{ steps.next-version.outputs.patch }} + 1))
          
          echo "value=$MAJOR.$MINOR.$PATCH-SNAPSHOT" >> $GITHUB_OUTPUT

      - name: Upgrade server pom version to ${{ steps.next-version.outputs.value }}
        shell: bash
        run: |
          mvn versions:set -DnewVersion=${{ steps.next-version.outputs.value }} --file server/pom.xml

      - name: Upgrade versions in README.md
        shell: bash
        run: |
          sed -i 's/https:\/\/img.shields.io\/badge\/Version-[0-9]\+\.[0-9]\+\.[0-9]-blue/https:\/\/img.shields.io\/badge\/Version-${{ steps.next-version.outputs.value }}-blue/g' README.md
          sed -i 's/image: heavynimbus\/mock-http-server:[0-9]\+\.[0-9]\+\.[0-9]/image: heavynimbus\/mock-http-server:${{ steps.next-version.outputs.value }}/g' README.md

      - name: Add & Commit
        id: add-and-commit
        uses: EndBug/add-and-commit@v9.1.4
        with:
          github_token: ${{ secrets.BOT_GITHUB_TOKEN }}
          message: "⬆️🚀 Upgrade version to ${{ steps.next-version.outputs.value }}"
          add: "server/pom.xml"

      - name: Create Tag ${{ steps.next-version.outputs.value }}
        uses: rickstaa/action-create-tag@v1
        id: "tag_create"
        with:
          tag: "v${{ steps.next-version.outputs.value }}"
          commit_sha: ${{ steps.add-and-commit.outputs.commit_sha }}

      - name: Push changes
        id: push-changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.BOT_GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          branch: ${{ github.ref_name }}
          tags: true
          force: true

      - name: Build Docker image ${{ steps.next-version.outputs.value }}
        env:
          NEXT_VERSION: ${{ steps.next-version.outputs.value }}
        run: |
          docker build -t heavy-mock-http-server:$NEXT_VERSION server

      - name: Push Docker image ${{ steps.next-version.outputs.value }}
        env:
          NEXT_VERSION: ${{ steps.next-version.outputs.value }}
        run: |
          docker tag "heavy-mock-http-server:$NEXT_VERSION" "heavynimbus/heavy-mock-http-server:$NEXT_VERSION"
          docker push "heavynimbus/heavy-mock-http-server:$NEXT_VERSION"

      - name: Set snapshot version
        run: |
          mvn versions:set -DnewVersion=${{ steps.snapshot-version.outputs.value }} --file server/pom.xml

      - name: Add & Commit snapshot version
        uses: EndBug/add-and-commit@v9.1.4
        with:
          github_token: ${{ secrets.BOT_GITHUB_TOKEN }}
          message: "⬆️🚀 Upgrade version to ${{ steps.snapshot-version.outputs.value }}"
          add: "server/pom.xml"

      - name: Push snapshot version
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.BOT_GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          branch: ${{ github.ref_name }}
          tags: true
          force: true
